{
  "name": "react-jobs-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch jobs.json --host 0.0.0.0 --port 10000 --static false"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}